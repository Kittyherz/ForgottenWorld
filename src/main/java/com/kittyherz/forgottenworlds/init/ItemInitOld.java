package com.kittyherz.forgottenworlds.init;

import com.kittyherz.forgottenworlds.ForgottenWorlds;
import net.minecraft.item.Item;
import net.minecraft.item.ItemGroup;
import net.minecraftforge.event.RegistryEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.registries.ObjectHolder;

import java.awt.*;

@Mod.EventBusSubscriber(modid = ForgottenWorlds.MOD_ID,bus = Mod.EventBusSubscriber.Bus.MOD)
@ObjectHolder(ForgottenWorlds.MOD_ID)
public class ItemInitOld {
/*

    public static final Item test_item = null;
    public static final Item scrap_metal = null;

    @SubscribeEvent
    public static void registerItems(final RegistryEvent.Register<Item> event) {
        event.getRegistry().register(new Item(new Item.Properties().group(ItemGroup.REDSTONE)).setRegistryName("test_item"));
        event.getRegistry().register(new Item(new Item.Properties().group(ForgottenWorlds.TAB)).setRegistryName("scrap_metal"));
        System.out.println("abcdefghij");
    }
*/
}
